import {createIconSetFromIcoMoon} from 'react-native-vector-icons';
// import fontelloConfig  from '../../configs/config.json';
import moonConfig from '../../configs/selection.json';

const Icon = createIconSetFromIcoMoon(moonConfig);
export default Icon;
