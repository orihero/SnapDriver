enum OrderStatus {
    ACCEPTED = 'accepted',
    ARRIVED = 'arrived',
    RATING = 'rating',
    WAITING = 'waiting',
    PROCESSING = 'processing',
    DONE = 'done',
    CANCELED = 'canceled',
}

export default OrderStatus;
