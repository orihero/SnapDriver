enum OrderStatus {
    ACCEPTED = 'accepted',
    ARRIVED = 'arrived',
    WAITING = 'waiting',
    PROCESSING = 'processing',
    DONE = 'done',
    CANCELED = 'canceled',
}

export default OrderStatus;
